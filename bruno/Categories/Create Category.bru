meta {
  name: Create Category
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Custom Category",
    "emoji": "🎯",
    "color": "#FF5733",
    "type": "EXPENSE"
  }
}
