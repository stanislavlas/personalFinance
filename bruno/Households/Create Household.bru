meta {
  name: Create Household
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/households
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "My Family"
  }
}
