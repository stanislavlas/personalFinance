meta {
  name: Delete Household
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/households/:householdId
  body: none
  auth: bearer
}

params:path {
  householdId: REPLACE_WITH_HOUSEHOLD_ID
}

auth:bearer {
  token: {{accessToken}}
}
