meta {
  name: Leave Household
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/households/:householdId/leave
  body: none
  auth: bearer
}

params:path {
  householdId: REPLACE_WITH_HOUSEHOLD_ID
}

auth:bearer {
  token: {{accessToken}}
}
