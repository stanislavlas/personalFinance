meta {
  name: Delete Account
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/auth/account
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "password": "password123"
  }
}
