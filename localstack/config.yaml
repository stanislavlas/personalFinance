name: "Personal Finance - LocalStack"
description: "LocalStack DynamoDB for Personal Finance app"
# Bump version on every push so HA shows "Update available" in the add-on store.
# Use MAJOR.MINOR.PATCH: patch for bug fixes, minor for new features.
version: "1.0.0"
slug: "personalfinance-localstack"
init: false
arch:
  - aarch64
  - amd64
ports:
  4566/tcp: 4566
ports_description:
  4566/tcp: "DynamoDB endpoint"
map:
  - data
options: {}
schema: {}
